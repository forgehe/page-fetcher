const request = require("request");
const fs = require("fs");
const args = process.argv.slice(2);

const fetcher = args => {
  //stuff
  const url = args[0];
  const saveTo = args[1];
};

fetcher(args);
